{
  "name": "note-u",
  "version": "0.10.0",
  "private": true,
  "description": "A URL-only, static note editor",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check storage.js && node --check calculator.js && node --check editor.js && node --check ui.js && node --check app.js"
  }
}
